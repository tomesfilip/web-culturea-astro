---
import Layout from '../layouts/Layout.astro';

import { SectionAbout } from '../sections/SectionAbout';
import { SectionMap } from '../sections/SectionMap';
import { SectionOrganizer } from '../sections/SectionOrganizer';
import { SectionTeam } from '../sections/SectionTeam';
import { SectionTopics } from '../sections/SectionTopics';
import { SectionProgram } from '../sections/SectionProgram';
import { SectionPartners } from '../sections/SectionPartners';
import { SectionLastYear } from '../sections/SectionLastYear';
import { SectionLanding } from '../sections/SectionLanding';
import { ScrollToTopButton } from '../components/buttons/ScrollTopButton';

import { CULTUREA_YEAR } from '../constants';

import '../styles/headingStyles.css';
---

<Layout title={`Culturea ${CULTUREA_YEAR}`}>
  <ScrollToTopButton />
  <SectionLanding slot="heroSection" />
  <SectionAbout slot="heroSection" />
  <!-- <SectionTopics /> -->
  <SectionProgram client:only="react" />
  <SectionLastYear client:only="react" />
  <SectionMap client:only="react" />
  <SectionTeam client:only="react" />
  <SectionPartners />
  <SectionOrganizer />
</Layout>

<style></style>
