import { TailSpin } from 'react-loader-spinner';

export const Loader = () => (
  <TailSpin
    height="80"
    width="80"
    color="#FF8400"
    ariaLabel="tail-spin-loading"
    radius="1"
    visible
  />
);
